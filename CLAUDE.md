# chandolkarim.github.io

정찬민(한국외대 아랍어과 · Business AI)의 개인 홈페이지. 수업(기술개발연구프로젝트) 실습용 저장소.

- 배포: GitHub Pages, `main` 브랜치 루트 → https://chandolkarim.github.io/
- `main`에 push하면 1~2분 안에 자동 반영된다. 빌드 도구·패키지 매니저 없음.

## 구조

| 파일 | 역할 |
|---|---|
| `index.html` | 한국어 페이지 |
| `ar.html` | 아랍어 페이지 (`dir="rtl"`) — 내용을 바꾸면 두 파일을 함께 수정 |
| `style.css` | 두 페이지가 공유하는 스타일 |
| `script.js` | 언어 전환 시 섹션 해시 유지, 스크롤 진행바, 현재 섹션 표시 |

## 수업 규칙 (지킬 것)

- 외부 폰트·외부 이미지·프레임워크·CDN 사용 금지. 시스템 폰트와 인라인 SVG만.
- 이력·수치·프로젝트 결과는 확인되지 않은 내용을 지어내지 않는다.
- 확인하지 않은 외부 링크는 넣지 않는다.
- 색 대비 WCAG AA(4.5:1) 이상, 390px 폭에서 가로 스크롤 없음, 제목 단계 h1→h2→h3 유지.
- 한국어 페이지를 고치면 아랍어 페이지도 같은 구조로 맞춘다. 지도 SVG는 RTL에서도 뒤집지 않는다.

## 로컬 확인

```bash
python3 -m http.server 8000
```

브라우저에서 http://localhost:8000 (파일을 직접 열어도 동작함).

## 작업 흐름

1. 파일 수정 → 브라우저에서 한국어·아랍어 페이지 모두 확인
2. `git add -A && git commit -m "..."`
3. `git push` → 배포 확인
